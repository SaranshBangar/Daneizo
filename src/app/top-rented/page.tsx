import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Top Rented Items | Daneizo", 
};

const Page = () => {
    return (
        <section>
            <p>Top rented</p>
        </section>
    );
}

export default Page;
